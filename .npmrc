# react-leaflet@5 declares a React 19 peer dependency, but this project is on
# React 18. The pairing works at runtime; this flag stops npm from erroring on
# the peer mismatch so `npm install` / `npm ci` succeed without a manual flag.
legacy-peer-deps=true
